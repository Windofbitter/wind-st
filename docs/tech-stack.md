# Tech Stack

- **Database**: SQLite for local development, with schema compatible for future PostgreSQL migration via ORM.
- **Backend**: Fastify with TypeScript, using an ORM (e.g., Drizzle or Prisma) to access the database and integrate MCP.
- **Frontend**: React (with Vite) for a component-based UI with drag-and-drop prompt blocks and editors.
